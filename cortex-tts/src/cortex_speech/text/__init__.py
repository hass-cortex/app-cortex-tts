"""The text pipeline: one locale per language, and the passes it runs.

`pipeline` is the entry point; `locales` defines what a language is; `zh/`
and `en/` are the languages written so far; `generic` is what any other
language gets.
"""
